# Зафиксированные версии для воспроизводимой сборки Docker-образа.
# Диапазоны совместимости объявлены в pyproject.toml; здесь — конкретные
# версии, на которых образ собран и проверен.
#
# Обновление: поднять версию, пересобрать образ, прогнать
#   python -m pytest tests/ -q
# Переход на mcp 2.x отдельной задачей: там FastMCP переименован в MCPServer.

mcp==1.30.0
pypdf==6.18.1
